[package]
name = "gthings-extraction"
version = "0.3.1"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Pure Rust HTML/PDF content extraction with heuristic quality scoring"

[dependencies]
gthings-common = { path = "../common", version = "0.3.2" }
serde = { workspace = true }
serde_json = { workspace = true }
regex = "1"
flate2 = "1"
scraper = "0.20"

[lints]
workspace = true
